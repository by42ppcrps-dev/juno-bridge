// Juno Bridge — point this at your deployed relay, then mirror the same host
// in manifest.json's host_permissions before loading the extension.
const JUNO_RELAY_URL = "https://YOUR-RELAY.workers.dev";
